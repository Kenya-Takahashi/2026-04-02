version: "3.8"

services:
  research-flow:
    build: .
    ports:
      - "3001:3000"
    volumes:
      - ./data:/app/data
    environment:
      DATABASE_PATH: /app/data/research-flow.db
      APP_TIME_ZONE: Asia/Tokyo
    restart: unless-stopped
